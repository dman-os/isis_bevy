/* #[derive(Debug, Clone, Component)]
pub struct SingleFormation {
    pub pattern: FormationPattern,
    pub slotting_strategy: SlottingStrategy,
}

#[derive(Debug, Component, Default)]
pub struct SingleFormationState {
    pub slots: FormationSlots,
    pub boid_strategies: HashMap<Entity, Entity>,
}

pub type Bundle =
    FlockStrategyBundleJumbo<SingleFormation, (SingleFormationState, FormationOutput)>;
 */
